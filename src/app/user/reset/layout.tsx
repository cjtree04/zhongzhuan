import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "重置密码 — Zhongzhuan Token",
  description: "完成 Zhongzhuan Token 密码重置流程。",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
