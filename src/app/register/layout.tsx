import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "注册 — Zhongzhuan Token",
  description: "注册 Zhongzhuan Token,加客服微信领 $20 美元额度。",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
